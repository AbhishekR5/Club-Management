<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="group_club_user" model="res.groups">
        <field name="name">Club User</field>
    </record>

    <record id="group_club_manager" model="res.groups">
        <field name="name">Club Manager</field>
        <field name="implied_ids" eval="[(4, ref('club_membership.group_club_user'))]"/>
        <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
    </record>

</odoo>